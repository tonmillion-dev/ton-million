import { TonClient } from '@ton/ton';
import { Address, Cell } from '@ton/core';
import { parseBoc } from '@ton/core/dist/boc/cell/serialization';
import { base64Decode } from '@ton/sandbox/dist/utils/base64';

describe("", () => {
    it("", async () => {

        const base64 = "49WsQu6ktcPtl7kXWOAF+5/vkpWFmjzApiPVjlyTl9I=";
        const encode = encodeURIComponent(base64);
        console.log(encode);
    });
});
